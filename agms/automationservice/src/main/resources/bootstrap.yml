spring:
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      name: automationservice
      profile: default
      fail-fast: false
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
